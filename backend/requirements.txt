fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pillow==10.1.0
ultralytics==8.3.231
python-dotenv==1.0.0
pydantic==2.5.0
aiofiles==23.2.1
httpx==0.25.2
# 注意：numpy 和 opencv-python 由 ultralytics 自动安装，不需要手动指定
# 这样可以避免版本冲突和 Python 3.12 兼容性问题

